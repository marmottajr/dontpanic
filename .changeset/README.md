# Changesets

This folder is managed by [changesets](https://github.com/changesets/changesets).

Add a changeset describing your change:

```bash
pnpm changeset
```

Then versioning + changelog generation happen via `pnpm changeset version`.
