{
  "name": "dontpanic",
  "version": "0.0.0",
  "private": true,
  "description": "DontPanic — a production-grade full-stack boilerplate. The answer to building any system. (Marvin would say: 'Here I am, brain the size of a planet, scaffolding your CRUD app.')",
  "packageManager": "pnpm@11.7.0",
  "engines": {
    "node": ">=22",
    "pnpm": ">=11"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "test:e2e": "turbo run test:e2e",
    "format": "prettier --write \"**/*.{ts,tsx,md,json,yml,yaml}\"",
    "format:check": "prettier --check \"**/*.{ts,tsx,md,json,yml,yaml}\"",
    "audit": "pnpm audit --audit-level high",
    "changeset": "changeset",
    "release": "changeset publish",
    "prepare": "husky"
  },
  "lint-staged": {
    "*.{ts,tsx,mjs,cjs,js,jsx,json,md,yml,yaml,css}": "prettier --write"
  },
  "devDependencies": {
    "@changesets/cli": "^2.31.0",
    "@commitlint/cli": "21.2.1",
    "@commitlint/config-conventional": "21.2.0",
    "husky": "^9.1.7",
    "lint-staged": "17.0.8",
    "prettier": "^3.9.5",
    "turbo": "^2.10.4",
    "typescript": "7.0.2"
  }
}
