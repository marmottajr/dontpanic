{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "types": ["jest", "node"],
    // Widened past the tsconfig's "./src" so the specs under test/ compile too.
    "rootDir": "."
  },
  "include": ["src/**/*.ts", "test/**/*.ts"]
}
